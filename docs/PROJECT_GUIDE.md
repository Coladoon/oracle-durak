# Полная инструкция по проекту «Дурак только на Oracle»

## 1. Что это за проект

Проект реализует серверную карточную игру «Дурак» только средствами Oracle
Database. Внешнее приложение и графический интерфейс не требуются: игроки
подключаются через SQL Developer, SQLcl или SQL*Plus, выполняют команды PL/SQL
и читают состояние из представлений.

Основная редакция содержит требования минимального и хорошего уровней:

- от 2 до 6 игроков;
- колоды 36 и 52 карты;
- подкидной и переводной варианты;
- первый ход по младшему козырю или детерминированно по seed;
- атака, защита, подбрасывание, перевод, пас и взятие;
- добор до шести карт, выход игроков и определение дурака;
- таймеры хода и завершение простаивающих партий;
- маскирование чужих карт;
- Daily, реплей, журнал и табло результатов.

Ботов, турниров, расширенной аналитики и настраиваемых домашних правил в
основной установке нет.

## 2. Как устроена система

Работа одной команды проходит по следующей цепочке:

```text
SQL Developer
    -> DURAK_CONSOLE или DURAK_API
    -> DURAK_ENGINE
    -> DURAK_RULES и DURAK_RANDOM
    -> таблицы Oracle
    -> защищённые представления
```

- `DURAK_CONSOLE` принимает короткие русские команды.
- `DURAK_API` определяет игрока по `SESSION_USER`, проверяет доступ и возвращает
  сообщения `УСПЕШНО` или `ОТКАЗ`.
- `DURAK_ENGINE` блокирует строку партии и атомарно изменяет состояние игры.
- `DURAK_RULES` проверяет бой карт и лимиты стола.
- `DURAK_RANDOM` строит воспроизводимый порядок колоды по seed.
- `DURAK_MAINTENANCE` и `DBMS_SCHEDULER` обрабатывают таймеры.
- представления показывают только разрешённую текущему пользователю информацию.

Карты не копируются между таблицами. Каждый экземпляр карты партии находится в
одной зоне: `TALON`, `HAND`, `ATTACK`, `DEFENSE` или `DISCARD`. Полный протокол записывается
в `DURAK_EVENT`, а перемещения карт — в `DURAK_CARD_MOVE`.

## 3. Состав каталогов

| Путь | Назначение |
|---|---|
| `install.sql` | чистая установка всех объектов |
| `reinstall.sql` | удаление старых объектов и повторная установка |
| `sql/` | таблицы, пакеты, представления и планировщик |
| `admin/` | выдача игрокам минимальных прав |
| `demo/` | готовые сценарии ручной игры |
| `tests/` | 78 автоматических проверок |
| `docs/` | документация и матрица требований |
| `reports/` | отчёты ЛР1 и ЛР2 |
| `versions/` | архив прежних редакций, не входящих в установку |

## 4. Быстрый запуск Oracle в Docker

Если Oracle уже установлен, этот раздел можно пропустить.

Создайте контейнер, заменив пароли своими значениями:

```bash
docker run -d \
  --name durak-oracle \
  -p 1521:1521 \
  -e ORACLE_PASSWORD='<пароль_SYS>' \
  -e APP_USER=DURAK_OWNER \
  -e APP_USER_PASSWORD='<пароль_DURAK_OWNER>' \
  -v durak-oracle-data:/opt/oracle/oradata \
  gvenzl/oracle-free:latest
```

Проверьте запуск:

```bash
docker logs -f durak-oracle
```

Продолжайте после сообщения `DATABASE IS READY TO USE!`. В дальнейшем контейнер
запускается командой:

```bash
docker start durak-oracle
```

Пароли не следует записывать в файлы проекта или добавлять в Git.

## 5. Подключение SQL Developer

Для владельца игры создайте подключение со следующими параметрами:

| Поле | Значение для Docker |
|---|---|
| Name | `durak-owner` |
| Username | `DURAK_OWNER` |
| Password | пароль из `APP_USER_PASSWORD` |
| Connection Type | `Basic` |
| Hostname | `localhost` |
| Port | `1521` |
| Тип адреса | `Service name` |
| Service name | `FREEPDB1` |
| Role | `default` |

Нажмите `Test`, затем `Connect`. Если соединение не устанавливается:

1. убедитесь, что контейнер запущен: `docker ps`;
2. дождитесь полной готовности базы в `docker logs durak-oracle`;
3. используйте `Service name = FREEPDB1`, а не `SID = xe`;
4. проверьте, что порт `1521` не занят другим Oracle;
5. не подключайтесь к службе `FREE`, потому что пользователь приложения создан
   в подключаемой базе `FREEPDB1`.

## 6. Установка игры

Откройте подключение `DURAK_OWNER`, затем откройте файл `install.sql` из корня
проекта и выполните его как сценарий клавишей `F5`.

```sql
@install.sql
```

Установка должна завершиться без ошибок компиляции. Для проверки объектов:

```sql
SELECT object_name, object_type, status
FROM user_objects
WHERE object_type IN ('PACKAGE', 'PACKAGE BODY', 'VIEW')
  AND status <> 'VALID'
ORDER BY object_type, object_name;
```

Ожидается `no rows selected`.

Повторная установка удаляет игровые данные:

```sql
@reinstall.sql
```

Поэтому `reinstall.sql` следует запускать только перед чистой демонстрацией или
если старая схема больше не нужна.

## 7. Создание пользователей игроков

Каждый человек должен играть из отдельного Oracle-пользователя. Подключитесь к
`FREEPDB1` пользователем `SYSTEM` или `SYS` и создайте, например, двух игроков:

```sql
CREATE USER PLAYER_ONE IDENTIFIED BY "<пароль_игрока_1>";
GRANT CREATE SESSION TO PLAYER_ONE;

CREATE USER PLAYER_TWO IDENTIFIED BY "<пароль_игрока_2>";
GRANT CREATE SESSION TO PLAYER_TWO;
```

Для партии на 3–6 человек аналогично создайте `PLAYER_THREE` и остальных.

Вернитесь в подключение `DURAK_OWNER` и выдайте каждому только разрешённые
права:

```sql
@admin/grant_player_access.sql PLAYER_ONE
@admin/grant_player_access.sql PLAYER_TWO
```

Сценарий выдаёт право выполнять `DURAK_API`, `DURAK_CONSOLE` и читать
защищённые представления. Прямого доступа к игровым таблицам и движку у игроков
нет.

Создайте в SQL Developer отдельное подключение для каждого пользователя:

- `player-one`: пользователь `PLAYER_ONE`;
- `player-two`: пользователь `PLAYER_TWO`;
- те же `localhost`, `1521` и `FREEPDB1`.

## 8. Создание обычной партии

В окне `PLAYER_ONE` выполните:

```sql
@demo/01_creator.sql DURAK_OWNER Михаил demo-seed-1
```

Сценарий:

1. регистрирует текущего Oracle-пользователя как игрока;
2. создаёт подкидную партию на 36 карт;
3. выводит `PLAYER_ID`, `GAME_ID` и текст результата.

Запишите `GAME_ID`. Например, если получено `2102`, во втором окне выполните:

```sql
@demo/02_joiner.sql DURAK_OWNER Анна 2102
```

Для 3–6 игроков каждый следующий пользователь выполняет тот же сценарий со
своим именем и тем же `GAME_ID`.

После подключения всех участников создатель выполняет:

```sql
@demo/03_start.sql DURAK_OWNER 2102
```

Движок создаст колоду, раздаст каждому по шесть карт, откроет козырь и определит
первого атакующего.

## 9. Просмотр состояния игры

В каждом окне игрока один раз переключите текущую схему:

```sql
ALTER SESSION SET CURRENT_SCHEMA = DURAK_OWNER;
```

Затем состояние можно смотреть одним запросом:

```sql
SELECT game_text,
       turn_text,
       hand_text,
       table_text,
       players_text,
       command_hint,
       last_event_text
FROM v_game_console
WHERE game_id = 2102;
```

`HAND_TEXT` для каждого подключения будет разным: игрок видит только свою руку.
Чужие карты показываются количеством.

Подробные представления:

```sql
SELECT * FROM v_game_status WHERE game_id = 2102 ORDER BY seat_no;
SELECT * FROM v_hand_mine WHERE game_id = 2102 ORDER BY rank_value, suit_code;
SELECT * FROM v_hand_public WHERE game_id = 2102 ORDER BY seat_no;
SELECT * FROM v_table WHERE game_id = 2102 ORDER BY pair_no;
```

Поле `MY_ALLOWED_ACTIONS` в `V_GAME_STATUS` и подсказка `COMMAND_HINT` в
`V_GAME_CONSOLE` сообщают, что текущему игроку разрешено сделать сейчас.

## 10. Как выполнять ходы

В каждом окне один раз создайте переменную для ответа:

```sql
VARIABLE message VARCHAR2(1000)
```

Доступные команды:

```sql
EXEC durak_console.play('ХОД 8H', :message)
EXEC durak_console.play('БИТО 9H 1', :message)
EXEC durak_console.play('ПОДКИНУТЬ 8C', :message)
EXEC durak_console.play('ПЕРЕВОД 8D', :message)
EXEC durak_console.play('ВЗЯТЬ', :message)
EXEC durak_console.play('ПАС', :message)
PRINT message
```

Коды мастей:

- `C` — трефы;
- `D` — бубны;
- `H` — червы;
- `S` — пики.

Примеры кодов карт: `6C`, `10H`, `QD`, `AS`.

Можно передать только карту:

```sql
EXEC durak_console.play('8H', :message)
PRINT message
```

Консоль сама выберет атаку, защиту или подбрасывание по текущей фазе. Для
защиты нескольких пар лучше явно указывать номер пары: `БИТО 9H 2`.

Справка:

```sql
EXEC durak_console.help(:message)
PRINT message
```

После каждой команды снова запрашивайте `V_GAME_CONSOLE`. Если команда
недопустима, данные не изменятся, а в `MESSAGE` будет указана причина отказа.

## 11. Правила хода

1. Атакующий кладёт первую карту.
2. Защитник бьёт её старшей картой той же масти либо козырем.
3. Козырь можно побить только старшим козырем.
4. После покрытия атакующая сторона может подбрасывать только достоинства,
   которые уже лежат на столе.
5. Число пар ограничено параметром `MAX_PAIRS` и исходным числом карт защитника.
6. Если все атакующие карты покрыты и атакующие пасуют, стол уходит в отбой.
7. Если защитник выбирает `ВЗЯТЬ`, карты стола переходят в его руку.
8. После раунда сначала добирает атакующая сторона, затем защитник.
9. Когда талон пуст, игрок с пустой рукой выходит и пропускается в дальнейшем.
10. Последний игрок с картами становится дураком.

В переводном варианте защитник до первой защиты может выполнить `ПЕРЕВОД`
картой того же достоинства. Следующий активный игрок становится защитником.

## 12. Создание партии с другими параметрами

Пример партии на 52 карты в переводном режиме со случайным первым ходом по seed:

```sql
VARIABLE game_id NUMBER
VARIABLE message VARCHAR2(1000)

BEGIN
  durak_api.create_game(
    p_game_id          => :game_id,
    p_message          => :message,
    p_deck_size        => 52,
    p_game_variant     => 'PEREVODNOY',
    p_first_move_mode  => 'SEEDED_RANDOM',
    p_turn_timeout_sec => 60,
    p_idle_timeout_min => 30,
    p_seed_text        => 'demo-seed-52'
  );
END;
/

PRINT game_id
PRINT message
```

Допустимые значения:

| Параметр | Значения |
|---|---|
| `P_DECK_SIZE` | `36` или `52` |
| `P_GAME_VARIANT` | `PODKIDNOY` или `PEREVODNOY` |
| `P_FIRST_MOVE_MODE` | `LOWEST_TRUMP` или `SEEDED_RANDOM` |
| `P_MAX_PAIRS` | по умолчанию 6 для 36 и 8 для 52 |
| `P_TURN_TIMEOUT_SEC` | `0` отключает таймер хода |
| `P_IDLE_TIMEOUT_MIN` | время завершения простаивающей партии |

При одинаковых параметрах и seed порядок карт будет одинаковым.

## 13. Таймеры

Фоновое задание проверяется запросом:

```sql
SELECT job_name, enabled, state
FROM user_scheduler_jobs
WHERE job_name = 'DURAK_MAINTENANCE_JOB';
```

Ожидается `ENABLED = TRUE`. После истечения времени:

- в ожидании первой атаки движок выбирает допустимую карту;
- защитник автоматически берёт карты;
- подбрасывающий автоматически пасует;
- длительно простаивающая партия получает статус `EXPIRED`.

Причина и источник `TIMEOUT` сохраняются в протоколе.

## 14. Журнал и реплей

Читаемый журнал:

```sql
SELECT log_line
FROM v_log_text
WHERE game_id = 2102
ORDER BY event_no;
```

Подробный реплей:

```sql
SELECT replay_step_no,
       event_no,
       event_type,
       actor_name,
       event_card_code,
       moved_card_code,
       from_zone,
       to_zone,
       phase_before,
       phase_after,
       event_message
FROM v_replay
WHERE game_id = 2102
ORDER BY replay_step_no;
```

Во время активной партии закрытые события другого игрока маскируются. После
завершения участникам доступна полная история.

## 15. Daily

Создание партии с раскладкой на определённую дату:

```sql
@demo/05_daily.sql DURAK_OWNER Михаил 2026-09-24
```

Другие игроки присоединяются через `demo/02_joiner.sql`. Одинаковые дата,
версия правил и параметры создают одинаковый Daily seed.

Просмотр раскладки дня:

```sql
SELECT *
FROM v_daily
ORDER BY daily_date DESC, daily_id DESC;
```

Результаты дня:

```sql
SELECT *
FROM v_daily_results
WHERE daily_id = <DAILY_ID>
ORDER BY daily_place, finish_place;
```

## 16. Общее табло

```sql
SELECT display_name,
       games_played,
       wins,
       losses,
       draws,
       avg_duration_sec,
       avg_rounds
FROM v_leaderboard
ORDER BY wins DESC, losses, display_name;
```

Табло формируется по завершённым партиям и не требует ручного пересчёта.

## 17. Автоматическая проверка

В подключении `DURAK_OWNER` выполните:

```sql
SET SERVEROUTPUT ON SIZE UNLIMITED
@tests/run_all.sql
```

Ожидаемый итог:

```text
--- Итого: успешно 78, ошибок 0 ---
```

Проверяются карты, правила боя, seed, маскирование данных, объекты схемы,
полный раунд, взятие, добор, таймеры, Daily, 2–6 игроков, колода 52, перевод и
пропуск вышедшего игрока.

Проверка двух одновременных сеансов описана в `tests/concurrency/README.md`.
Ожидаемый результат второй сессии — отказ `ORA-20702`, а не двойное действие.

## 18. Частые сообщения

### «У игрока нет карты 10C»

Запрошенной карты нет в вашей руке. Посмотрите `HAND_TEXT` или `V_HAND_MINE` и
используйте один из показанных кодов.

### «Сейчас нельзя начинать атаку»

Сейчас ход другого игрока или игра находится в фазе защиты/подбрасывания.
Проверьте `TURN_TEXT`, `COMMAND_HINT` и `MY_ALLOWED_ACTIONS`.

### «У пользователя нет активной партии»

Пользователь не создал и не подключился к партии либо партия завершена.
Подключитесь через `JOIN_GAME` или вызовите перегрузку консоли с `GAME_ID`:

```sql
EXEC durak_console.play(2102, 'ХОД 8H', :message)
```

### «Игрок уже участвует в активной партии»

По заданию один Oracle-пользователь может иметь только одну активную игровую
сессию. Завершите или отмените предыдущую партию.

### `ORA-20702`

Другая сессия сейчас изменяет ту же партию. Обновите состояние и повторите
команду после освобождения блокировки.

### Файл запускается как один оператор и появляются ошибки на `/`

SQL-файлы необходимо выполнять как сценарий клавишей `F5`, а не как отдельный
оператор `Ctrl+Enter`.

## 19. Порядок функциональной проверки

1. Показать отсутствие невалидных объектов.
2. Открыть два подключения игроков.
3. Создать подкидную партию на 36 карт с известным seed.
4. Подключить второго игрока и запустить раздачу.
5. Показать разные `HAND_TEXT` в двух окнах и одинаковый открытый стол.
6. Выполнить атаку, защиту, подбрасывание и пас либо взятие.
7. Показать добор, журнал и реплей.
8. Создать переводную партию или партию на 52 карты.
9. Показать Daily и `V_LEADERBOARD`.
10. Запустить `tests/run_all.sql` и показать результат 78/78.

## 20. Где смотреть подтверждение требований

- `docs/IMPLEMENTATION_STATUS.md` — чек-лист минимального и хорошего уровней;
- `tests/run_all.sql` — единый автоматический прогон;
- `reports/Михаил_ЛР1_Дурак_Oracle.docx` — постановка и проектирование;
- `reports/Михаил_ЛР2_Дурак_Oracle.docx` — реализация и тестирование.

## 21. Назначение каждого файла проекта

### 21.1. Корневые файлы

| Файл | Назначение |
|---|---|
| `README.md` | Краткое описание возможностей, требований и порядка установки. |
| `install.sql` | Последовательно создаёт схему, справочник карт, пакеты, представления и фоновое задание. |
| `reinstall.sql` | Вызывает удаление объектов, затем выполняет чистую установку. Все игровые данные удаляются. |

### 21.2. Каталог sql

| Файл | Создаваемые объекты и назначение |
|---|---|
| `sql/00_drop.sql` | Безопасно удаляет задание, представления, пакеты, таблицы и последовательность. |
| `sql/01_schema.sql` | Создаёт `DURAK_ID_SEQ`, 13 таблиц, PK, FK, CHECK, UNIQUE и индексы. |
| `sql/02_cards_seed.sql` | Заполняет справочник `DURAK_CARD` всеми 52 картами и отмечает 36-карточную колоду. |
| `sql/03_random_pkg.sql` | Создаёт `DURAK_RANDOM`: seed, SHA-256-хеширование и детерминированный порядок колоды. |
| `sql/04_rules_pkg.sql` | Создаёт `DURAK_RULES`: сравнение карт, лимиты атаки и обход активных игроков. |
| `sql/05_views.sql` | Создаёт защищённые представления состояния, рук, журнала, реплея, Daily и табло. |
| `sql/05_console_views.sql` | Создаёт текстовые представления `V_GAME_CONSOLE` и `V_LOG_TEXT`. |
| `sql/06_engine_pkg.sql` | Основной игровой автомат `DURAK_ENGINE`; изменяет состояние партии атомарно. |
| `sql/07_api_pkg.sql` | Публичный пакет `DURAK_API`; идентифицирует игрока и вызывает движок. |
| `sql/07_console_pkg.sql` | Пакет `DURAK_CONSOLE`; переводит короткие русские команды в вызовы API. |
| `sql/08_maintenance_pkg.sql` | Обрабатывает просроченные ходы и завершает простаивающие партии. |
| `sql/09_scheduler.sql` | Создаёт `DURAK_MAINTENANCE_JOB` с запуском каждые 10 секунд. |

Порядок файлов важен. При обычной установке их следует запускать только через
`install.sql`, а не по отдельности.

### 21.3. Каталог admin

| Файл | Назначение |
|---|---|
| `admin/grant_player_access.sql` | Выдаёт игроку `EXECUTE` на API и консоль, а также `SELECT` на защищённые представления. |

Сценарий не выдаёт игроку прямых прав на таблицы и `DURAK_ENGINE`.

### 21.4. Каталог demo

| Файл | Назначение |
|---|---|
| `demo/01_creator.sql` | Регистрирует создателя и создаёт обычную партию. |
| `demo/02_joiner.sql` | Регистрирует следующего пользователя и подключает его к партии. |
| `demo/03_start.sql` | Запускает раздачу после подключения участников. |
| `demo/04_actions.sql` | Содержит примеры прямых вызовов игрового API. |
| `demo/05_daily.sql` | Создаёт Daily-партию на указанную дату. |
| `demo/07_human_console.sql` | Настраивает переменные и показывает текстовую консоль партии. |

### 21.5. Каталог tests

| Файл | Что проверяет |
|---|---|
| `tests/00_test_support.sql` | Создаёт пакет тестовой инфраструктуры `DURAK_TEST`. |
| `tests/01_cards_test.sql` | 52 карты, короткую колоду 36 и уникальность кодов. |
| `tests/02_rules_test.sql` | Правила боя, козыри и лимиты стола. |
| `tests/03_random_test.sql` | Воспроизводимость seed и порядка колоды. |
| `tests/04_views_test.sql` | Маскирование рук, журнала, реплея и ошибок. |
| `tests/05_objects_test.sql` | Наличие и статус пакетов, представлений и scheduler job. |
| `tests/06_engine_test.sql` | Раздачу, полный раунд, добор, взятие, таймер и простой. |
| `tests/07_daily_test.sql` | Daily seed и запись результатов. |
| `tests/08_modes_test.sql` | 2–6 игроков, колоду 52, переводной режим и пропуск вышедших. |
| `tests/run_all.sql` | Запускает весь комплект автоматических тестов. |
| `tests/concurrency/*` | Проверяет конфликт двух одновременных сеансов. |

### 21.6. Документация и отчёты

| Файл | Назначение |
|---|---|
| `docs/PROJECT_GUIDE.md` | Основная инструкция по установке, устройству и использованию. |
| `docs/USAGE.md` | Краткая памятка по ручной игре. |
| `docs/IMPLEMENTATION_STATUS.md` | Соответствие требованиям минимального и хорошего уровней. |
| `docs/ER_MODEL.md` | Подробное описание сущностей, полей, ключей и кратностей. |
| `docs/ER_DURAK.drawio` | Редактируемая ER-диаграмма для app.diagrams.net. |
| `docs/Руководство_по_запуску_и_игре_Дурак_Oracle.docx` | Иллюстрированная инструкция Word. |
| `docs/Руководство_по_запуску_и_игре_Дурак_Oracle.pdf` | Версия иллюстрированной инструкции PDF. |
| `reports/Михаил_ЛР1_Дурак_Oracle.docx` | Отчёт по проектированию базы данных. |
| `reports/Михаил_ЛР2_Дурак_Oracle.docx` | Отчёт по реализации и тестированию. |
| `versions/` | Архив прежних редакций; при установке не используется. |

## 22. Пакеты, функции и процедуры

### 22.1. DURAK_API — публичный интерфейс

| Процедура | Назначение |
|---|---|
| `REGISTER_PLAYER` | Регистрирует текущего `SESSION_USER` или возвращает существующую запись. |
| `CURRENT_PLAYER` | Возвращает `PLAYER_ID` текущего пользователя. |
| `CURRENT_GAME` | Возвращает активный `GAME_ID` пользователя. |
| `CREATE_GAME` | Создаёт обычную партию с выбранными параметрами. |
| `CREATE_DAILY_GAME` | Создаёт партию с детерминированным Daily seed. |
| `JOIN_GAME` | Добавляет игрока на свободное место в лобби. |
| `START_GAME` | Проверяет состав, перемешивает колоду, раздаёт карты и начинает первый раунд. |
| `ATTACK` | Выполняет первую атаку. |
| `DEFEND` | Покрывает выбранную атакующую карту. |
| `THROW_IN` | Подбрасывает карту допустимого достоинства. |
| `PASS_THROW` | Передаёт или завершает очередь подбрасывания. |
| `TAKE_CARDS` | Объявляет взятие карт защитником. |
| `TRANSFER_ATTACK` | Переводит атаку следующему игроку до начала защиты. |
| `HEARTBEAT` | Обновляет время общей активности, но не продлевает таймер текущего хода. |
| `CANCEL_GAME` | Отменяет лобби; доступно только создателю до запуска. |

Все процедуры возвращают текст в параметре `P_MESSAGE`. Бизнес-ошибка не
оставляет частично изменённых данных: API откатывается к savepoint и записывает
отказ в `DURAK_ERROR`.

### 22.2. DURAK_CONSOLE — команды человека

| Вызов | Назначение |
|---|---|
| `PLAY(command, message)` | Находит активную партию пользователя и выполняет команду. |
| `PLAY(game_id, command, message)` | Выполняет команду в явно указанной партии. |
| `HELP(message)` | Возвращает список поддерживаемых команд. |

Поддерживаемые команды: `ХОД`, `БИТО`, `ПОДКИНУТЬ`, `ПЕРЕВОД`, `ВЗЯТЬ`,
`ПАС`, `ПОМОЩЬ`. Код карты без слова-команды автоматически интерпретируется по
текущей фазе.

### 22.3. DURAK_ENGINE — игровой автомат

Движок выполняет `CREATE_GAME`, `JOIN_GAME`, `START_GAME`, все игровые действия,
таймеры и завершение. Внутренние процедуры отвечают за блокировку строки партии,
проверку участника, перемещение карт, добор, запись событий, переход к следующему
раунду и определение результата. Игроки не вызывают движок напрямую.

### 22.4. DURAK_RULES — правила

| Функция | Назначение |
|---|---|
| `DEFAULT_MAX_PAIRS` | Возвращает 6 для колоды 36 и 8 для колоды 52. |
| `EFFECTIVE_ATTACK_LIMIT` | Ограничивает число атак параметрами партии и рукой защитника. |
| `CAN_BEAT_VALUES` | Сравнивает масти и ранги без обращения к таблицам. |
| `CAN_BEAT` | Проверяет, может ли одна карта побить другую. |
| `ACTIVE_PLAYER_COUNT` | Считает активных участников партии. |
| `NEXT_ACTIVE_SEAT` | Находит следующего активного игрока по кругу. |
| `RANK_PRESENT_ON_TABLE` | Проверяет наличие достоинства на столе для подбрасывания. |

### 22.5. DURAK_RANDOM — детерминированная случайность

| Функция | Назначение |
|---|---|
| `GENERATED_SEED` | Создаёт seed, если пользователь его не указал. |
| `NORMALIZE_SEED` | Приводит seed к каноническому виду. |
| `HASH_TEXT` | Возвращает SHA-256 для текста. |
| `GAME_SEED_HASH` | Формирует хеш seed с учётом параметров партии. |
| `CARD_SHUFFLE_KEY` | Вычисляет сортировочный ключ карты. |
| `DAILY_SEED` | Строит одинаковый seed по дате и версии правил. |
| `SEEDED_POSITION` | Выбирает воспроизводимую позицию игрока. |

### 22.6. DURAK_MAINTENANCE — обслуживание

| Процедура | Назначение |
|---|---|
| `PROCESS_DUE_ACTIONS` | Выполняет автоматические действия просроченных ходов. |
| `EXPIRE_IDLE_GAMES` | Завершает партии без активности. |
| `RUN_ONCE` | Один цикл обеих операций; вызывается планировщиком. |

## 23. Представления и видимость данных

| Представление | Содержимое |
|---|---|
| `V_CURRENT_PLAYER` | Запись текущего Oracle-пользователя. |
| `V_GAME_STATUS` | Параметры партии, роли, таймеры и счётчики рук. |
| `V_TABLE` | Пары атаки и защиты, их статус и допустимые действия. |
| `V_HAND_MINE` | Открытые карты только текущего пользователя. |
| `V_HAND_PUBLIC` | Количество карт у всех участников без раскрытия чужих рук. |
| `V_LOG` | Доступная пользователю часть протокола. |
| `V_REPLAY` | Пошаговые события и перемещения карт. |
| `V_LEADERBOARD` | Победы, поражения, ничьи и средние показатели. |
| `V_DAILY` | Daily-конфигурации и число результатов. |
| `V_DAILY_RESULTS` | Табло выбранного дня. |
| `V_ERRORS` | Последние отказы текущего пользователя. |
| `V_GAME_CONSOLE` | Сводное текстовое состояние партии одним запросом. |
| `V_LOG_TEXT` | Журнал в виде готовых строк. |

Маскирование основано на `SESSION_USER`. Команда
`ALTER SESSION SET CURRENT_SCHEMA = DURAK_OWNER` меняет только разрешение имён и
не превращает игрока во владельца схемы.

## 24. Сущности базы данных

В основной схеме 13 таблиц:

1. `DURAK_PLAYER` — зарегистрированные игроки;
2. `DURAK_CARD` — справочник карт;
3. `DURAK_DAILY` — конфигурация раскладки дня;
4. `DURAK_GAME` — партия и её текущее состояние;
5. `DURAK_GAME_PLAYER` — участники конкретной партии;
6. `DURAK_ACTIVE_SESSION` — одна активная партия на пользователя;
7. `DURAK_GAME_CARD` — положение каждой карты партии;
8. `DURAK_ROUND` — состояние раунда;
9. `DURAK_TABLE_PAIR` — пары атака/защита;
10. `DURAK_EVENT` — неизменяемый протокол событий;
11. `DURAK_CARD_MOVE` — перемещения карт для реплея;
12. `DURAK_ERROR` — автономный журнал отказов;
13. `DURAK_DAILY_RESULT` — результаты Daily.

Полный перечень полей, типов, PK, FK, уникальных ограничений и кратностей
приведён в `docs/ER_MODEL.md`. Редактируемая диаграмма находится в
`docs/ER_DURAK.drawio` и открывается через **File → Import From → Device** на
сайте `https://app.diagrams.net`.

## 25. Полный порядок развёртывания и настройки

### 25.1. Создание шести Oracle-пользователей

Выполните от `SYSTEM` в `FREEPDB1`:

```sql
CREATE USER DURAK_P1 IDENTIFIED BY "Player1_2026!";
CREATE USER DURAK_P2 IDENTIFIED BY "Player2_2026!";
CREATE USER DURAK_P3 IDENTIFIED BY "Player3_2026!";
CREATE USER DURAK_P4 IDENTIFIED BY "Player4_2026!";
CREATE USER DURAK_P5 IDENTIFIED BY "Player5_2026!";
CREATE USER DURAK_P6 IDENTIFIED BY "Player6_2026!";

GRANT CREATE SESSION TO DURAK_P1;
GRANT CREATE SESSION TO DURAK_P2;
GRANT CREATE SESSION TO DURAK_P3;
GRANT CREATE SESSION TO DURAK_P4;
GRANT CREATE SESSION TO DURAK_P5;
GRANT CREATE SESSION TO DURAK_P6;
```

Проверка выполняется в том же административном подключении:

```sql
SELECT username, account_status
FROM dba_users
WHERE username LIKE 'DURAK_P%'
ORDER BY username;
```

### 25.2. Установка и выдача прав

В корне распакованного проекта откройте Worksheet `DURAK_OWNER` и запустите
клавишей `F5`:

```sql
@install.sql

@admin/grant_player_access.sql DURAK_P1
@admin/grant_player_access.sql DURAK_P2
@admin/grant_player_access.sql DURAK_P3
@admin/grant_player_access.sql DURAK_P4
@admin/grant_player_access.sql DURAK_P5
@admin/grant_player_access.sql DURAK_P6
```

### 25.3. Настройка игровых окон

В каждом подключении `DURAK_P1`–`DURAK_P6` один раз выполните:

```sql
SET SERVEROUTPUT ON SIZE UNLIMITED
ALTER SESSION SET CURRENT_SCHEMA = DURAK_OWNER;
VARIABLE message VARCHAR2(1000)
```

Проверка контекста:

```sql
SELECT SYS_CONTEXT('USERENV','SESSION_USER') AS session_user,
       SYS_CONTEXT('USERENV','CURRENT_SCHEMA') AS current_schema
FROM dual;
```

`SESSION_USER` должен оставаться конкретным игроком, а `CURRENT_SCHEMA` —
`DURAK_OWNER`.

### 25.4. Параметры партии

При вызове `CREATE_GAME` задаются:

| Параметр | Допустимые значения |
|---|---|
| `P_DECK_SIZE` | 36 или 52 |
| `P_GAME_VARIANT` | `PODKIDNOY` или `PEREVODNOY` |
| `P_FIRST_MOVE_MODE` | `LOWEST_TRUMP` или `SEEDED_RANDOM` |
| `P_MAX_PAIRS` | 1–6 для колоды 36, 1–8 для 52 |
| `P_TURN_TIMEOUT_SEC` | 0 отключает таймер; иначе 1–86400 секунд |
| `P_IDLE_TIMEOUT_MIN` | 1–10080 минут |
| `P_SEED_TEXT` | строка до 128 байт |

Значение таймера штатно задаётся при создании. Публичной процедуры изменения
параметров уже запущенной партии нет, поскольку это изменило бы условия игры.

### 25.5. Досрочное завершение

Создатель может отменить только ещё не запущенную партию:

```sql
EXEC durak_api.cancel_game(<GAME_ID>, :message)
PRINT message
```

Активная партия завершается по правилам либо автоматически по
`P_IDLE_TIMEOUT_MIN`. Прямое изменение `DURAK_GAME` не рекомендуется: оно
обходит протокол, очистку сессий и проверки конкурентности.

## 26. Почему в прежней версии присутствовал Python

Python использовался только как вспомогательный инструмент подготовки готовых
файлов Word, PDF и иллюстраций. Код игры никогда не выполнялся на Python и не
зависел от него. Установка, правила, хранение данных, таймеры и интерфейс
полностью реализованы на Oracle SQL и PL/SQL. Генераторы удалены из финальной
версии репозитория; готовые документы сохранены.
