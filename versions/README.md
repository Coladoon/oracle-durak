# Сохранённые версии проекта

- `01_original` — исходный интерфейс через `DURAK_API` и базовые представления.
- `02_human_console` — выбранный упрощённый интерфейс через `DURAK_CONSOLE`,
  `V_GAME_CONSOLE` и `V_LOG_TEXT`.

Основной каталог проекта соответствует версии `02_human_console`.
