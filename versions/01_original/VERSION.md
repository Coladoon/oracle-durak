# Версия 1 — исходный SQL-интерфейс

Полная версия проекта до добавления упрощённой консоли.

Игрок работает через `DURAK_API`, `V_GAME_STATUS`, `V_TABLE`, `V_HAND_MINE`
и `V_LOG`. Установка: `@install.sql`, полная проверка: `@tests/run_all.sql`.
